
     pre_r($result->fetch_assoc());
pre_r($result->fetch_assoc());

if isset($_REQUEST['delete'])
{
   $id=$_REQUEST['delete'];
    $mysqli->query("Delete from course where")
    
    
    
}

if isset($_REQUEST['delete'])
{
   $id=$_REQUEST['delete'];
$mysqli->query("Delete from Course where ID=$id");    
}
<tr>
<td><h3>Rating options</h3></td>
<td><h3>Group or Indivividual</h3></td>
</tr>
<td><input type=radio name=fillin value=fillin> Fill in Rating Criteria</td>
<td><input type=radio name=fillin value=students> Let students choose rating criteria</td>


$conn=mysqli_connect("localhost","root","root","Greenlights");
    if (!$conn)
        die ("Could not connect to the database. Please try later");